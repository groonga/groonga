# TODO: We'll add a small expected file after we add JSON functions.

#@require-feature simdjson

table_create Notes TABLE_NO_KEY
column_create Notes data COLUMN_SCALAR JSON

# [256.times.inject({}) {|result, i| result[i.to_s] = i; result}] + # For skipping size8
# 65535.times.collect {|i| {i.to_s => i}} # For size32
#@generate-series 0 0 Notes '{"data" => ([256.times.inject({}) {|result, i| result[i.to_s] = i; result}] + 65535.times.collect {|i| {i.to_s => i}}).to_json}'

select Notes

dump
