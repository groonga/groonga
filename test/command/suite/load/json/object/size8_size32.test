# TODO: We'll add a small expected file after we add JSON functions.

#@require-feature simdjson

table_create Notes TABLE_NO_KEY
column_create Notes data COLUMN_SCALAR JSON

# 255.times.collect {|i| {i.to_s => i}} + # For size8
# [65536.times.inject({}) {|result, i| result[i.to_s] = i; result}] + # For skipping size16
# 255.step(65535).collect {|i| {i.to_s => i}} # For size32
#@generate-series 0 0 Notes '{"data" => (255.times.collect {|i| {i.to_s => i}} + [65536.times.inject({}) {|result, i| result[i.to_s] = i; result}] + 255.step(65535).collect {|i| {i.to_s => i}}).to_json}'

select Notes

dump
